extends ../template/layout

block content
  .row
    .small-12.columns
      #artists
        each artist in artists
          .song
            a(href='/artists/#{artist.id}'): img.art(src=artist.photo)
            h5.subheader= artist.name

