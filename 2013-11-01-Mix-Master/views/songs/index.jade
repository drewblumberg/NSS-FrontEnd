extends ../template/layout

block content
  .row
    .small-12.columns
      #songs
        each song in songs
          .song
            a(href='/songs/#{song.id}'): img.art(src=song.coverArt)
            h5.subheader= song.title

